<!DOCTYPE html>
<html>
  <head>
    <title>AnimalsApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/b84f170730.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
<header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
        <div class="container">
          <%= link_to "みんなの動物大全", root_path, class: "navbar-brand" %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
                <% if user_signed_in? %>
                  <li>
                    <%= link_to "Home", user_path(current_user.id), class: "fas fa-dog nav-link text-light" %>
                  </li>
                  <li>
                    <%= link_to "Users", users_path, class: "fas fa-cat nav-link text-light" %>
                  </li>
                  <li>
                    <%= link_to "Animals", animals_path, class: "fas fa-book-open nav-link text-light" %>
                  </li>
                  <li>
                    <%= link_to "Logout", destroy_user_session_path, metod: :delete, class: "fas fa-sign-out-alt nav-link text-light"%>
                  </li>
                <% else %>
                  <li>
                    <%= link_to "Home", root_path, class: "fas fa-dog nav-link text-light" %>
                  </li>
                  <li>
                    <%= link_to "About", home_about_path, class: "fas fa-cat nav-link text-light" %>
                  </li>
                  <li>
                    <%= link_to "sign up", new_user_registration_path, class: "fas fa-user-plus nav-link text-light" %>
                  </li>
                  <li>
                    <%= link_to "login", new_user_session_path, class: "fas fa-sign-in-alt nav-link text-light" %>
                  </li>
                <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <p><%= notice %></p>
    <%= yield %>
  </body>
  <footer>
    <div class="container mt-5">
      <div class="row">
        <div class="mx-auto">
          <p>CopyRight IppeiSuzuki.inc</p>
        </div>
      </div>
    </div>
  </footer>
</html>
